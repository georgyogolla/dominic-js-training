  // XOR (exclusive OR) operator
  console.log(true ^ true); // false // 1 ^ 1 = 0
  console.log(true ^ false); // true // 1 ^ 0 = 1
  console.log(false ^ true); // true // 0 ^ 1 = 0
  // If the bits are the same e.g 1 and 1 then th result will always be 0 which is false in boolean 
  // I the bits are different e.g 1 and 0 then the result will always be 1 which is true in boolean 
  